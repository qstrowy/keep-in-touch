SUPABASE_URL=###
SUPABASE_PUBLISHABLE_KEY=###

# Server-only Worker secrets. Configure preview values outside the repository;
# never expose these through client code or commit real values.
OPENROUTER_API_KEY=###
OPENROUTER_MODEL=###
OPENROUTER_PROVIDER=###
